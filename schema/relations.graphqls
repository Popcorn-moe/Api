enum RelationStatus {
    FRIEND,
    BLOCKED
    RECEIVE_INVITE,
    SEND_INVITE
}

type Relation {
    id: ID!
    status: RelationStatus!
    by: User!
    to: User!
    date: Date!
}

extend type Query {

}

extend type Mutation {

}
